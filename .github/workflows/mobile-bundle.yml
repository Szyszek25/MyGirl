name: Polka mobile bundle smoke test
on:
  push:
    branches: [main]
    paths:
      - 'App.js'
      - 'app.json'
      - 'package.json'
      - 'src/**'
      - '.github/workflows/mobile-bundle.yml'
  pull_request:
    paths:
      - 'App.js'
      - 'app.json'
      - 'package.json'
      - 'src/**'
      - '.github/workflows/mobile-bundle.yml'
  workflow_dispatch:

jobs:
  bundle:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Install dependencies
        run: npm install --no-audit --no-fund
      - name: Verify Expo dependency compatibility
        run: npx expo install --check
      - name: Expo doctor
        run: npx expo-doctor
      - name: Bundle iOS JavaScript
        run: npx expo export --platform ios --output-dir /tmp/polka-ios-export
      - name: Bundle Android JavaScript
        run: npx expo export --platform android --output-dir /tmp/polka-android-export
